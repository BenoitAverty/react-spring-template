# react-spring-template

React + Spring Boot + Jwt Auth

## Development 
```bash
cd server
./gradlew bootRun
cd ../client
yarn # (or npm install)
yarn run start # (or npm run start)
```

## Build

```bash
cd client
yarn
yarn run build
cd ../server
./gradlew build
```
